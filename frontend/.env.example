# URL base do backend/ FastAPI. Em dev, uvicorn roda em 8000 por padrão
# (ver backend/.env.example). Em produção, aponte pra onde o backend/
# estiver publicado.
VITE_API_BASE_URL=http://localhost:8000
