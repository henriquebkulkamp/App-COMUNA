fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
# greenlet: dependência de tempo de execução obrigatória de sqlalchemy.ext.asyncio
# (a ponte sync->async da engine async usa greenlet_spawn mesmo com driver
# nativamente async como asyncpg) — sem isso, qualquer AsyncEngine.connect()
# falha com "the greenlet library is required".
greenlet==3.5.5
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
